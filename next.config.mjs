/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,  // Ativa o modo estrito do React
  productionBrowserSourceMaps: false,  // Desativa os source maps na produção
};

export default nextConfig;
