/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,  // Ativa o modo estrito do React
  productionBrowserSourceMaps: true,  // Desativa os source maps na produção
};

export default nextConfig;
